// 定义一个方法
printNumber(num aNumber) {
  print('The number is $aNumber');
}

// 主函数，这是程序执行的入口
main() {
  // 定义并初始化一个变量
  var number = 86;
  // 调用一个方法
  printNumber(number);

  int lineCount;
  assert(lineCount == 1);
}
